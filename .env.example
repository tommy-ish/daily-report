# Database
DATABASE_URL="postgresql://username:password@localhost:5432/daily_report?schema=public"

# Application
NODE_ENV="development"
PORT=3000

# Session (REQUIRED - Must be at least 32 characters)
# Generate a strong random secret using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="your-secret-key-change-this-in-production-must-be-at-least-32-characters"

# CSRF (Optional - Currently not used, reserved for future use)
CSRF_SECRET="your-csrf-secret-change-this-in-production"
